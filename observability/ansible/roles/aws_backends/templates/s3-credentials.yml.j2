---
# ═══════════════════════════════════════════════════════════════════════════════
# S3 backends for the OpenShift observability stack.
#
#   Generated by  observability/ansible/aws-prereqs.yml
#   AWS account   {{ aws_account_number }}
#   Region        {{ aws_region }}
#
# CONTAINS LIVE AWS SECRET ACCESS KEYS. Mode 0600, gitignored. Hand it to the
# cluster admin over a channel you trust, or encrypt it in place:
#
#   ansible-vault encrypt s3-credentials.yml
#
# cluster.yml reads an encrypted file transparently with --ask-vault-pass.
# Re-running aws-prereqs.yml reuses the keys listed here rather than rotating
# them, so this file stays valid.
# ═══════════════════════════════════════════════════════════════════════════════
observability_s3_backends:
{{ _minted | to_nice_yaml(indent=2) | indent(2, true) }}
